# Cloudflare credentials for tools/crawl.mjs, which writes the snapshot the page reads.
# Copy to .env and fill in. .env is gitignored and must stay that way.

# Workers & Pages > Overview, right sidebar
CF_ACCOUNT_ID=

# the SY namespace id, same value as kv_namespaces in worker/wrangler.jsonc
CF_KV_NAMESPACE_ID=4e31fbb9611f46b0b69c66ddf0e50550

# My Profile > API Tokens > Create Token > Custom token.
# One permission is enough: Account > Workers KV Storage > Edit.
# Scope it to this account and nothing else.
CF_API_TOKEN=

# Optional. The defaults assume an ordinary connection, where a full crawl of all
# 203 tickers takes about 150 seconds and nothing is refused.
#
# 120 is not clean on every connection. Dexscreener limits by IP, and on a machine
# already running other Dexscreener clients a full crawl came back with 53 requests
# refused and 20 tickers left on the previous snapshot, every drop counted under
# rateLimited. 250 crawled it clean, for about 30 seconds more. If a run reports
# dropped above zero, read dropReasons in /api/status: rateLimited means raise this,
# the other three mean something else is wrong.
# PACE_MS=120
# DEADLINE_MS=900000
