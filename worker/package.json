{
  "name": "stockyard-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "seed": "wrangler kv key put --binding SY snapshot --path ../data/page.json --remote"
  },
  "devDependencies": {
    "wrangler": "^4.28.0"
  }
}
